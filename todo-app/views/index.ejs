<!-- ... (your existing HTML code) ... -->

<script>
    // Example tasks data
    var overdueTasks = [
        { title: 'Task 1' },
        { title: 'Task 2' }
    ];

    var dueTodayTasks = [
        { title: 'Task 3' },
        { title: 'Task 4' }
    ];

    var dueLaterTasks = [
        { title: 'Task 5' },
        { title: 'Task 6' }
    ];

    // Calculate counts
    var overdueCount = overdueTasks.length;
    var dueTodayCount = dueTodayTasks.length;
    var dueLaterCount = dueLaterTasks.length;

    // Update counts in the placeholders
    document.getElementById('count-overdue').textContent = overdueCount;
    document.getElementById('count-due-today').textContent = dueTodayCount;
    document.getElementById('count-due-later').textContent = dueLaterCount;
</script>

<%- include("footer.ejs") %>
</body>
</html>
